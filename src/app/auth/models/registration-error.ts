const defaultError = 'Sorry... you cannot register at the moment';

export const registrationError = {
  duplicate_emailaddress: 'Dit e-mail adres is al bekend in ons systeem.',
  default: defaultError
};

